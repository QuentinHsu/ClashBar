# CatBar 业务功能清单

> 基于 `dev` 分支源码分析，仅包含业务层面功能，不涉及技术架构细节。

---

所有的改动，都必须保证内存占用不能上升。

## 1. 代理管理（Proxy Tab）

- 查看所有代理分组及当前选中节点
- 在分组内切换代理节点（行内选择）
- 对节点/分组进行延迟测试（healthcheck URL）
- 按延迟排序节点
- 显示/隐藏被标记为隐藏的分组
- 根据代理模式自动过滤分组显示：
  - **Rule 模式**：显示用户自定义分组
  - **Global 模式**：仅显示 GLOBAL 分组
  - **Direct 模式**：不显示分组

## 2. 节点浏览（Nodes Tab）

- 查看代理提供者（Proxy Provider）的节点列表、订阅信息（流量用量 / 到期时间）
- 按提供者分组展开/折叠节点列表
- 搜索节点名称（跨提供者和本地节点）
- 查看节点详情：类型、存活状态、延迟、测试历史
- 对单个节点进行延迟测试
- 单独刷新指定提供者
- 显示各提供者的最后更新时间

## 3. 规则浏览（Rules Tab）

- 查看规则总数与 Rule Provider 集合数
- 按 Provider/分类分组浏览规则（可展开折叠）
- 搜索过滤规则
- 查看规则详情：类型 → 匹配目标 → 对应策略
- 手动刷新规则提供者（支持按组或全部刷新）
- 每次最多保留 100 条规则

## 4. 连接监控（Connections Tab）

- 实时查看活跃网络连接列表
- 按域名 / IP / 主机名搜索
- 按协议过滤（TCP / UDP / Other）
- 排序方式：默认、最新、最旧、上传流量、下载流量、总流量
- 查看连接详情：
  - 目标主机/IP
  - 匹配规则类型及 payload（DOMAIN-SUFFIX、IP-CIDR 等）
  - 网络协议
  - 上游代理链路（代理分组 → 选中节点）
  - 上传/下载流量
  - 连接开始时间
- 关闭单个连接
- 关闭所有连接
- 复制主机地址或连接 ID 到剪贴板
- 内存保留最近 120 条连接，单独追踪总数

## 5. 日志查看（Logs Tab）

- 查看系统日志（CatBar 应用日志 + Mihomo 核心日志）
- 按来源过滤（CatBar / Mihomo / 全部）
- 按级别过滤（Info / Warning / Error）
- 文本搜索日志
- 复制单条日志消息或完整日志条目
- 清空全部日志
- 自动识别协议标签（HTTP / UDP / DNS）并高亮
- 时间戳相对显示（如 "5m ago"）

## 6. 设置（Settings Tab）

### 应用设置

- 语言切换：English / 简体中文
- 外观模式：跟随系统 / 浅色 / 深色
- 菜单栏显示样式：仅图标 / 图标+速度 / 仅速度
- 自动启动核心：应用启动时自动启动代理核心
- 自动恢复核心：断网时停止核心，恢复网络后自动重启
- 开机自启：系统登录时自动启动 CatBar

### 代理配置

- Allow LAN：允许局域网设备通过本机代理
- IPv6 支持
- TCP Concurrent：允许并发 TCP 连接
- 端口配置：HTTP / SOCKS / Mixed（默认 7890）/ Redir / TProxy
- 保存端口配置并应用到运行中的核心

### TUN 模式

- 启用/禁用 TUN 模式（全局透明代理，需 root 权限）
- 请求管理员权限（对 mihomo 二进制设置 setuid）

### 日志级别

- 设置核心日志级别：silent / error / warning / info / debug

### 核心管理

- 在 Finder 中打开核心目录
- 显示核心路径
- 检测核心状态（mihomo 二进制是否存在可用）

## 7. 状态栏与快捷操作

### 状态栏显示

- 图标状态指示：运行中(绿色闪电) / 降级(空心闪电) / 启动中(旋转) / 失败(警告) / 已停止(划线闪电)
- 可选显示实时上传/下载速度
- 显示活跃连接数
- 显示核心内存占用
- 显示实时与累计流量

### 快捷操作（弹出面板）

- 切换配置文件
- 导入本地配置（YAML）
- 导入远程配置（订阅 URL）
- 更新所有订阅
- 重新扫描配置目录
- 系统代理开关
- TUN 模式开关
- 复制代理命令（生成 shell export 命令）

### 头部操作

- 启动 / 停止 / 重启核心
- 重新加载配置
- 切换代理目标机器（本地 / 远程）
- 管理远程机器
- 退出应用

## 8. 远程机器管理

- 添加 / 编辑 / 删除远程代理机器
- 配置项：名称、主机地址、端口（默认 9090）、密钥、HTTPS 开关
- 检测远程机器连通性
- 查看连接状态（运行中 / 不可达 / 检测中）
- 在本地与远程代理目标之间切换
- 打开远程机器 Web 面板（自动拼接 URL 参数）

## 9. 核心与维护操作

- **清空 DNS 缓存** - 清除代理核心中的 DNS 缓存
- **清空 FakeIP 缓存** - 清除 DNS FakeIP 映射缓存
- **更新 GEO 数据** - 下载最新地理位置数据库
- **更新核心** - 升级 mihomo 到最新版本
- **检查核心版本** - 显示当前核心版本号
- **重启核心 API** - 重新初始化核心 API（不完全重启）

## 10. 配置文件管理

- 选择活跃配置文件（YAML）
- 重新加载配置（软重启）
- 强制从磁盘重新加载并应用配置
- 从本地文件导入配置
- 从远程 URL 导入订阅配置
- 批量更新所有远程订阅
- 在 Finder 中显示配置目录
- 启动前使用 `mihomo -t` 验证配置
- 显示订阅最后更新时间、流量用量与到期信息
- 自动为导入配置补全 `.yaml` 扩展名

## 11. 核心运行时生命周期

### 运行状态

- **Stopped** - 已停止，可启动
- **Starting** - 启动中，API 尚未就绪
- **Running (Healthy)** - 运行中，API 正常响应
- **Running (Degraded)** - 运行中但 API 异常或网络问题
- **Failed** - 核心崩溃或初始化失败
- **Restarting** - 正在关闭并重启

### 网络监测

- 监听网络可达性（在线 / 离线）
- 网络断开时可选停止核心
- 网络恢复后自动重启核心

## 12. 提供者刷新与同步

- 刷新代理提供者（更新节点列表）
- 刷新规则提供者（更新规则集）
- 支持单个或批量刷新
- 可取消进行中的刷新操作
- 进度展示（X/Y 已更新）
- 每个提供者独立的成功/失败状态及时间戳
- 切换配置 / 重启核心 / 退出应用时自动取消刷新

## 13. 实时数据流

- **流量流** - 持续上传/下载速度指标（自适应频率）
- **内存流** - 核心内存占用更新
- **连接流** - 活跃连接列表周期性更新（200ms 合并去抖）
- **日志流** - 错误日志条目实时推送
- 面板打开时高频率，关闭时低频率（自适应采集策略）

## 14. 系统代理集成

- 设置为 macOS 全局 HTTP/HTTPS 代理
- 撤销系统代理（恢复直连）
- 启动时自动修复系统代理配置
- 配置变更时自动同步代理端口
- 前置条件：Helper 服务已安装、后台活动权限已授予、从 /Applications 运行

## 15. 错误处理与恢复

- **核心启动失败** - 配置验证错误、二进制缺失、权限问题
- **系统代理失败** - Helper 进程异常、后台活动被阻止
- **TUN 模式失败** - 权限不足、二进制不可执行
- **配置验证错误** - YAML 语法错误或无法识别的字段
- **远程配置下载错误** - 网络超时、URL 无效、大小超限
- **提供者更新失败** - 部分/全部失败并支持重试追踪
- 根据错误类型提供修复建议

## 16. 键盘快捷键

| 快捷键 | 功能 |
|---|---|
| `Cmd+Shift+R` | 核心主操作（重新加载/重启） |
| `Cmd+Shift+.` | 停止核心 |
| `Cmd+Option+T` | 切换 TUN 模式 |
| `Cmd+Option+1~6` | 切换到 Proxy/Nodes/Rules/Connections/Logs/Settings |
| `Cmd+Shift+K` | 刷新当前标签页 |
| `Cmd+Option+U` | 检查 CatBar 更新 |
| `Cmd+Option+Shift+C` | 复制代理命令 |
| `Cmd+Option+Shift+L` | 复制全部日志 |
| `Cmd+Option+Shift+Delete` | 清空全部日志 |

## 17. 国际化

- 支持语言：English、简体中文
- 覆盖 300+ 条用户可见字符串
- 所有 UI 标签、按钮、错误信息、状态指示均已本地化

## 18. 订阅信息展示

- 流量用量条（已用上传 / 已用下载 / 总额度）
- 到期倒计时（剩余天数 / 今日到期 / 已过期）
- 永久订阅标识
